# 任务 02：消息表分区与会话标识接入

> 系列：《会话列表与离线同步方案》分阶段执行的第二份任务书
> 来源：主方案 §4.6 / §4.7 / §6.1（写入部分）/ §13 缺陷 H I K L M N O P Q / §15（分区提前）
> 状态：待执行（**依赖任务 01 完成**：`single_session_uuid` / 常量 / 新表就位）
> 定位：消息表全量改造 —— 加会话列 + 哈希分区 + 修复消息表相关全部 bug。结束时可全绿

---

## 1. 目标与边界

### 1.1 本任务做什么

| # | 内容 | 对应缺陷 |
|---|------|----------|
| 1 | `chat_message_record` 重写：加 `session_uuid`、`bigserial`、分区、索引 | K / M / N |
| 2 | `group_message_record` 重写：分区 + 拉取索引 | P |
| 3 | 消息落库路径写入 `session_uuid`（服务端派生） | — |
| 4 | 修复 `/msg/get_chat_record` 的 SQL 列名与分页参数 bug | H / I |
| 5 | 群聊 `select_unread` 游标与排序同列 + 参数化 limit + `has_more` | O / Q |
| 6 | `nano_id` 补唯一约束（分区形态：会话内唯一） | L |
| 7 | 删除 `chat_message_record_fail`（纯 DDL 死表）与 `raw_insert` 死代码 | — |

### 1.2 本任务不做什么

| 不做 | 归属 |
|------|------|
| `session` 表的写入（聚合/触发时机） | 任务 03 / 06 |
| `select_unread_by_time` / `get_unread_chat_record` 的游标逻辑重构（缺陷 A–G） | 任务 04 / 05 —— 该路径整体废弃，修了也是白修 |
| 已读上报路径变更（缺陷 J） | 任务 04 |
| 删 `chat_message_record_read` / `group_member.last_read_msg_id` | 任务 04（游标归一，有生产引用） |
| `/msg/get_chat_record` 入参改 `session_uuid` | 后续接口任务（本任务只修 bug，不动契约） |
| 客户端任何改动 | 任务 07（`has_more` 为新增可选字段，向后兼容，客户端无需立即消费） |

### 1.3 ⚠️ 决策注记：分区提前到本任务

主方案 §15.7 建议「功能开发期不分区，上线前再加」。**本任务按用户决策直接落地分区**，理由与影响：

- 未上线无数据，现在分区 = 只改 DDL 定义，成本与"上线前再加"完全相同，且**免掉后续 DROP 重建的切换流程**（§15.5 记录的 `IF NOT EXISTS` 静默跳过陷阱不会再遇到）
- 代价：任务 02–08 全程在分区表上开发。rbatis 对分区透明，仅调试时直接看数据要多一层（查父表即可，PG 自动路由）
- **任务 09 的定义因此变更**：从「加分区」变为「分区行为验证 + 数据清理任务」（见 §12）

---

## 2. 前置条件

- [ ] 任务 01 已完成：`single_session_uuid` 可用、`session` / `user_session` 表已建、uuid `v5` feature 已开
- [ ] 开发库中旧表需要 **DROP 重建**（分区表不能用 `IF NOT EXISTS` 从普通表原地转换，见 §3.4）

```sql
-- 开发库手动执行一次（未上线，无数据可保）
DROP TABLE IF EXISTS chat_message_record CASCADE;
DROP TABLE IF EXISTS group_message_record CASCADE;   -- 它的 FK 指向 group_info, 先删自身无碍
DROP TABLE IF EXISTS chat_message_record_fail CASCADE;
```

---

## 3. DDL 重写

### 3.1 `crates/entity/ddl/chat_message_record.sql`（全量重写）

```sql
-- public.chat_message_record 表定义
-- 按会话哈希分区: 所有同步/未读查询都带 session_uuid, 单分区剪枝

-- 删除表
-- DROP TABLE chat_message_record;

CREATE TABLE IF NOT EXISTS chat_message_record (
    id           bigserial   NOT NULL,          -- 自增id(原 serial4, 消除21亿上限)
    session_uuid uuid        NOT NULL,          -- 会话标识: 单聊由用户对 v5 派生
    nano_id      varchar(64) NOT NULL,          -- 消息主键(会话内唯一)
    send_user    uuid        NOT NULL,          -- 发送人id
    recv_user    uuid        NOT NULL,          -- 接收用户id
    text_type    int4        NOT NULL DEFAULT 0, -- 消息类型，0-文本，1-图片
    "timestamp"  int8        NOT NULL,          -- 创建时间(毫秒)
    raw          bytea       NULL,              -- 二进制数据
    -- 分区表约束必须含分区键(§15.3): nano_id 全局唯一性降级为会话内唯一,
    -- 跨会话由 nanoid!() 生成端保证 + 客户端去重兜底
    CONSTRAINT chat_message_record_pk      PRIMARY KEY (id, session_uuid),
    CONSTRAINT chat_message_record_nano_uk UNIQUE (nano_id, session_uuid)
) PARTITION BY HASH (session_uuid);

-- 16 个哈希分区
DO $$
BEGIN
    FOR i IN 0..15 LOOP
        EXECUTE format(
            'CREATE TABLE IF NOT EXISTS chat_message_record_p%s
             PARTITION OF chat_message_record
             FOR VALUES WITH (MODULUS 16, REMAINDER %s)',
            lpad(i::text, 2, '0'), i);
    END LOOP;
END $$;

-- 按会话游标拉取; INCLUDE timestamp 使 7 天窗口过滤免回表(任务05消费)
CREATE INDEX IF NOT EXISTS idx_chat_msg_pull
    ON public.chat_message_record (session_uuid, id) INCLUDE ("timestamp");

-- 未读数统计(我收到的, 任务06消费)
CREATE INDEX IF NOT EXISTS idx_chat_msg_recv
    ON public.chat_message_record (recv_user, session_uuid, id);

COMMENT ON TABLE  public.chat_message_record               IS '单对单聊天记录(按会话哈希分区)';
COMMENT ON COLUMN public.chat_message_record.id           IS '自增id(bigserial)';
COMMENT ON COLUMN public.chat_message_record.session_uuid IS '会话标识, 单聊由用户对v5派生, 双向对称';
COMMENT ON COLUMN public.chat_message_record.nano_id      IS '消息主键(会话内唯一)';
COMMENT ON COLUMN public.chat_message_record.send_user    IS '发送人id';
COMMENT ON COLUMN public.chat_message_record.recv_user    IS '接收用户id';
COMMENT ON COLUMN public.chat_message_record.text_type    IS '消息类型，0-文本，1-图片';
COMMENT ON COLUMN public.chat_message_record."timestamp"  IS '创建时间(毫秒)';
COMMENT ON COLUMN public.chat_message_record.raw          IS '二进制数据';
```

> **删除的旧索引**：`chat_message_record_create_at_idx`（timestamp 单列）、`chat_message_record_send_user_idx`（send,recv,timestamp）。前者被 `idx_chat_msg_pull` 的 INCLUDE 覆盖场景，后者被新索引取代（双向 OR 查询将随任务 04/05 消亡，期间性能由 seq scan 承担 —— 开发期可接受）。
>
> **`ddl.rs` 兼容性**：`split_sql_statements` 支持美元引用块（`keeps_dollar_quoted_block_whole` 测试已覆盖），`DO $$ ... $$` 作为单条语句执行 ✓。

### 3.2 `crates/entity/ddl/01_group_tables.sql`（仅改 group_message_record 段）

```sql
-- 群消息表（读扩散，只存 1 份）—— 改为按群哈希分区
CREATE TABLE IF NOT EXISTS group_message_record (
    -- 分区表约束必须含分区键(§15.3)
    id BIGSERIAL,
    nano_id VARCHAR(64) NOT NULL,
    group_uuid UUID NOT NULL REFERENCES group_info(group_uuid),
    send_user UUID NOT NULL,
    timestamp BIGINT NOT NULL,
    raw BYTEA NOT NULL,
    msg_type SMALLINT DEFAULT 1,
    recalled BOOLEAN DEFAULT FALSE,
    PRIMARY KEY (id, group_uuid),
    UNIQUE (nano_id, group_uuid)
) PARTITION BY HASH (group_uuid);

-- 16 个哈希分区
DO $$
BEGIN
    FOR i IN 0..15 LOOP
        EXECUTE format(
            'CREATE TABLE IF NOT EXISTS group_message_record_p%s
             PARTITION OF group_message_record
             FOR VALUES WITH (MODULUS 16, REMAINDER %s)',
            lpad(i::text, 2, '0'), i);
    END LOOP;
END $$;

-- 新增: 与单聊对称的拉取索引(游标+排序同列, 7天窗口免回表)
CREATE INDEX IF NOT EXISTS idx_group_msg_pull
    ON group_message_record (group_uuid, id) INCLUDE ("timestamp");

-- 保留原索引(会话内按时间翻历史, offset 分页)
CREATE INDEX IF NOT EXISTS idx_group_msg_group_time ON group_message_record(group_uuid, timestamp DESC);
CREATE INDEX IF NOT EXISTS idx_group_msg_send_user ON group_message_record(send_user);
```

> **删除**：`idx_group_msg_nano_id`（被 `UNIQUE (nano_id, group_uuid)` 的隐式索引取代，且原单列索引在分区表上无法服务跨分区唯一性）。
>
> **外键**：`group_message_record REFERENCES group_info(group_uuid)` 保留 —— 分区键已包含在外键列中，PG 允许分区表引用普通表。

> ⚠️ 原 `id BIGSERIAL PRIMARY KEY` 写法改为 `id BIGSERIAL` + 表级 `PRIMARY KEY (id, group_uuid)`，语义相同（bigserial 仍自动建序列）。

### 3.3 删除 `crates/entity/ddl/chat_message_record_fail.sql`

纯 DDL 死表：全仓库无 Rust 实体、无写入、无消费者（`send_msg_record_failure` 是 `#[allow(dead_code)]`）。

### 3.4 分区表幂等陷阱（必须知晓）

`CREATE TABLE IF NOT EXISTS ... PARTITION BY HASH` 遇到**已存在的普通表**会**静默跳过**，随后 `CREATE TABLE ... PARTITION OF` 因父表不是分区表而报错。因此：

- 开发库执行本任务前**必须先跑 §2 的 DROP**
- CD 全新环境无此问题；但若某环境曾部署过未分区版本，重放 DDL 前需人工 DROP

---

## 4. 模型改造（entity crate）

### 4.1 `ChatMessageRecord` 加字段

`crates/entity/src/models/chat_entity/chat_message_record.rs:8-16`：

```rust
#[derive(Clone, Deserialize, Serialize, Debug)]
pub struct ChatMessageRecord {
    pub id: Option<i64>,
    pub session_uuid: Uuid,        // ← 新增(非 Option, 对应 NOT NULL)
    pub nano_id: Option<String>,
    pub timestamp: Option<i64>,
    pub raw: Bytes,
    pub text_type: Option<u32>,
    pub send_user: Uuid,
    pub recv_user: Uuid,
}
```

### 4.2 删除死代码 `raw_insert`

同文件 `:65-86` 的 `raw_insert` 手工 SQL **无任何调用者**，且不含 `session_uuid`（留着就是给未来埋雷 —— 谁调用谁 insert 失败）。整个函数连同注释块删除。

### 4.3 三处构造点同步更新

| # | 位置 | 改动 |
|---|------|------|
| 1 | `crates/quic_service/src/msg_service/process_msg_service.rs:380-388` | 生产落库，见 §5 |
| 2 | `crates/entity/src/tests.rs:320` | roundtrip 单测构造，加 `session_uuid: uuid("...")` |
| 3 | `crates/quic_service/tests/external_server_integration_test.rs:321-330` | 集成测试构造，加 `session_uuid`（用 `single_session_uuid(&send, &recv)` 现算，与生产路径同源） |

`GroupMessageRecord` 模型**无需改动** —— `group_uuid` 字段已存在，PK/UK 形态变化对 `crud!` 透明。

---

## 5. 落库路径写入 session_uuid

`crates/quic_service/src/msg_service/process_msg_service.rs:374-391`：

```rust
pub async fn add_user_chat_record(
    core: &CoreState,
    text_quic_msg: TextQuicMsg,
) -> Result<(), anyhow::Error> {
    // TODO kafka转发消息ck批量写入
    let rb = &core.db;
    let send_user = text_quic_msg.send_user.parse::<Uuid>()?;
    let recv_user = text_quic_msg.recv_user.parse::<Uuid>()?;
    let session_uuid = common::utils::session_uuid::single_session_uuid(&send_user, &recv_user);
    let chat_msg = ChatMessageRecord {
        id: None,
        session_uuid,
        nano_id: Some(text_quic_msg.nano_id),
        timestamp: Some(text_quic_msg.timestamp),
        raw: Bytes::from(text_quic_msg.raw),
        text_type: Some(text_quic_msg.text_type as u32),
        send_user,
        recv_user,
    };
    ChatMessageRecord::insert(rb, &chat_msg).await?;
    Ok(())
}
```

要点：

- 先解析两个 Uuid 再派生（原代码在字段处内联 parse，提前解析顺带消除重复）
- `quic_service` 已依赖 `common` ✓，`session_uuid` 模块由任务 01 提供
- 落库失败仍只打日志、仍回 ACK —— **at-most-once 有意取舍，本任务不碰**（主方案 §6.4）

> 本任务**不写 `session` 表**（聚合策略归任务 03/06）。落库路径仅新增一列写入。

---

## 6. Bug 修复（H / I / O / Q）

### 6.1 缺陷 H + I：`/msg/get_chat_record` 修复

`crates/entity/src/models/chat_entity/chat_message_record.rs:21-31`（SQL）：

```sql
-- 改前: order by created_at —— 该列不存在, 一调就报错
"select * from chat_message_record where ((send_user = #{send_user} and recv_user = #{recv_user}) or (send_user = #{recv_user} and recv_user = #{send_user})) order by created_at limit #{size} offset #{start}"

-- 改后: 游标与排序同列(§3 设计原则)
"select * from chat_message_record where ((send_user = #{send_user} and recv_user = #{recv_user}) or (send_user = #{recv_user} and recv_user = #{send_user})) order by id limit #{size} offset #{start}"
```

`crates/http_service/src/http_service/chat_service/service/text_msg_service.rs:24-25`（参数）：

```rust
// 改前: size 与 start 都取 page_num, page_size 被忽略
let start = base_page.page_num.unwrap_or(0);
let size = base_page.page_num.unwrap_or(10);

// 改后
let start = base_page.page_num.unwrap_or(0);
let size = base_page.page_size.unwrap_or(10);
```

> 双向 OR 查询保留（该接口契约不动）。旧索引已删，此查询开发期走 seq scan —— 可接受，任务 05 后该接口自然转型。

### 6.2 缺陷 O：群聊 `select_unread` 游标与排序同列 + 参数化

`crates/entity/src/models/group_entity/group_message_record.rs:56-64`：

```rust
// 改前: id 游标 + timestamp 排序(不同列) + limit 100 硬编码
#[rbatis::py_sql(
    "select * from group_message_record where group_uuid = #{group_uuid} and id > #{last_read_msg_id} order by timestamp asc limit 100"
)]
async fn select_unread(rb: &dyn Executor, group_uuid: &Uuid, last_read_msg_id: i64) -> Vec<GroupMessageRecord> {}

// 改后: 排序与游标同列; limit 参数化(供 has_more 的 size+1 探测)
#[rbatis::py_sql(
    "select * from group_message_record where group_uuid = #{group_uuid} and id > #{last_read_msg_id} order by id asc limit #{size}"
)]
async fn select_unread(
    rb: &dyn Executor,
    group_uuid: &Uuid,
    last_read_msg_id: i64,
    size: u32,
) -> Vec<GroupMessageRecord> {}
```

**调用点同步**（3 处，全部加 `size` 实参）：

| 位置 | 说明 |
|------|------|
| `crates/http_service/src/http_service/group_service/group_service.rs:648` | `get_group_message_history_service` 游标模式，传 `dto.size + 1`（见 §6.3） |
| `crates/http_service/src/http_service/group_service/group_service.rs:683` | 未读数统计路径，传原语义 `100`（后续任务 05 重构） |
| `crates/quic_service/src/msg_service/group_msg_service.rs:405` | 群离线同步路径，传 `100`（后续任务 05 重构） |

### 6.3 缺陷 Q：`has_more` 显式返回

**VO**（`crates/http_service/src/http_service/group_service/group_vo/group_message_vo.rs`）：

```rust
#[derive(Debug, Deserialize, Serialize)]
pub struct GroupMessageVO {
    pub nano_id: String,
    pub group_uuid: String,
    pub send_user: String,
    pub timestamp: i64,
    pub raw: Vec<u8>,
    pub msg_type: i16,
    pub recalled: bool,
    /// 是否还有更多消息(新增字段, Option 向后兼容, 老客户端可忽略)
    pub has_more: Option<bool>,
}
```

**service**（`get_group_message_history_service`，游标模式分支，`group_service.rs:646+`）：

```
查询时传 size + 1:
    messages = select_unread(rb, group_uuid, cursor, dto_size + 1)
    has_more = messages.len() > dto_size
    if has_more { messages.truncate(dto_size) }
    VO 填充 has_more
```

offset 模式分支（`select_by_group`）同样处理：`start + size` 查 `size + 1` 判断截断。

**所有构造 `GroupMessageVO` 的位置**同步补 `has_more` 字段（`group_service.rs` 内 VO 构造点，rg `GroupMessageVO {` 可全部定位）。

> 客户端 `pull_group_messages` 暂不消费 `has_more` —— 新增可选字段向后兼容，客户端接入在任务 07。

---

## 7. 测试

### 7.1 单测（更新 + 新增）

| 测试 | 位置 | 内容 |
|------|------|------|
| `chat_message_record_roundtrip...`（更新） | `entity/src/tests.rs:318+` | 构造加 `session_uuid`，roundtrip 含新字段 |
| `select_unread_orders_by_id`（新增） | `group_message_record.rs` 模块或 tests.rs | py_sql 字符串断言含 `order by id asc`（低成本防回归：直接断言宏内 SQL 文本） |
| `page_size_not_page_num`（新增） | `text_msg_service.rs` 或 http_service 单测 | `BasePageDTO{page_num: Some(2), page_size: Some(30)}` → start=2, size=30 |

### 7.2 集成测试

| 测试 | 位置 | 内容 |
|------|------|------|
| `single_chat_message_persists_session_uuid`（新增） | `quic_service/tests/external_server_integration_test.rs` | 走 QUIC 发一条单聊 → 查 `chat_message_record` 最新行 → `session_uuid == single_session_uuid(send, recv)` 且**双向再发一条结果相同**（对称性在库内成立） |
| 已有测试构造更新 | 同文件 `:321` | `ChatMessageRecord` 构造加 `session_uuid`（现算派生，与生产同源） |
| DDL 集成（增强） | `entity/tests/ddl_integration_test.rs` | 表清单不变（`chat_message_record` / `group_info` 已在）；**新增分区校验**：`SELECT count(*) FROM pg_inherits WHERE inhparent = 'chat_message_record'::regclass` = 16（两张表各断一次） |
| 手动回归 | — | `/msg/get_chat_record/{uuid}` 调用返回 200（改前必 500）；`/group/chat/message/history` 游标模式返回 `has_more` |

### 7.3 运行命令

```bash
cargo check --workspace --all-targets
cargo clippy --workspace --all-targets -- -D warnings
cargo +nightly fmt --all --check
cargo test --workspace --tests
cargo test -p entity --test ddl_integration_test -- --ignored
cargo test -p quic_service --test external_server_integration_test -- --ignored
```

---

## 8. 验收（DoD）

- [ ] 开发库已 DROP 重建，两张消息表为分区表（`\d+ chat_message_record` 显示 16 个分区）
- [ ] `pg_inherits` 校验通过（两张表各 16 分区）
- [ ] `chat_message_record.session_uuid` NOT NULL，`nano_id` 会话内唯一（`UNIQUE (nano_id, session_uuid)`）
- [ ] 落库集成测试绿：`session_uuid` 与派生值一致、双向对称
- [ ] `/msg/get_chat_record` 不再报 SQL 错误（缺陷 H），`page_size` 生效（缺陷 I）
- [ ] 群聊 `select_unread` 为 `order by id asc`（缺陷 O），history 返回 `has_more`（缺陷 Q）
- [ ] `raw_insert` 与 `chat_message_record_fail.sql` 已删除，`rg "raw_insert|chat_message_record_fail"` 无残留
- [ ] `cargo clippy -D warnings` 无告警
- [ ] 旧链路回归：单聊/群聊消息收发正常（本任务未改任何协议与推送路径，仅落库多一列）

---

## 9. 本任务产出（供后续任务消费）

| 产出 | 形式 | 消费方 |
|------|------|--------|
| 分区版 `chat_message_record`（含 `session_uuid`） | DDL + 模型 | 任务 05（同步查询）、06（未读数）、09（分区验证） |
| 分区版 `group_message_record` | DDL | 同上 |
| `idx_chat_msg_pull` / `idx_group_msg_pull` | DDL 索引 | 任务 05（`id > cursor order by id asc` 走 index-only scan） |
| 落库时派生 `session_uuid` | `add_user_chat_record` | 任务 03（聚合按会话分组） |
| `has_more` 字段 | `GroupMessageVO` | 任务 07（客户端拉取循环） |

---

## 10. 系列任务规划（更新）

| 任务 | 主题 | 状态 |
|------|------|------|
| 01 | 会话表与会话标识 | 文档就绪 |
| **02（本档）** | 消息表分区与会话标识 + 消息表 bug（H I K L M N O P Q） | 文档就绪 |
| 03 | session 数据访问层（upsert / 游标更新 / 聚合） | 待写 |
| 04 | 游标归一（`/session/read` + `/session/synced` + 删旧已读表） | 待写 |
| 05 | 离线同步 `/session/sync`（7 天窗口，消费 `has_more` 服务端侧） | 待写 |
| 06 | 会话列表 `/session/list` + 聚合触发点 | 待写 |
| 07 | 客户端改造（含 `has_more` 消费、v5 复刻 + 跨端向量断言） | 待写 |
| 08 | 历史接口入参 `session_uuid` 化 + HTTP `Client` 复用 | 待写 |
| 09 | **重新定义**：分区行为验证（剪枝 EXPLAIN、跨分区清理 SQL、约束形态回归）+ 数据清理任务 | 待写 |
