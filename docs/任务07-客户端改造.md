# 任务 07：客户端改造（同步 / 已读 / 会话列表）

> 系列：《会话列表与离线同步方案》分阶段执行的第七份任务书
> 来源：主方案 §11 全部 / §12 阶段 10–11
> 状态：待执行（**依赖服务端任务 01–06 全部完成**）
> 定位：系列里唯一在 `only-talk-app` 仓库动代码的任务。完成后删除对旧接口的全部依赖，服务端桥接层进入待废弃状态

---

## 1. 目标与边界

### 1.1 本任务做什么

| # | 内容 |
|---|------|
| 1 | v5 派生复刻 + **跨端向量断言**（兑现任务 01 埋的对齐机制） |
| 2 | 本地三张表加列：`chat_session`（`session_uuid` / `synced_id`）、`chat_record` / `group_chat_record`（`server_id`） |
| 3 | 同步流程重写：`/session/sync` 空响应终止循环 + `/session/synced` 回报；删 `get_unread_message` / `pull_group_messages` |
| 4 | 已读上报切换：`/msg/add_read_chat_record`（旧 DTO）→ `/session/read`（新契约），保留 10 秒批量节流骨架 |
| 5 | 会话列表刷新：`/session/list` 同步本地 `chat_session` |
| 6 | TS 类型定义（`packages/types`，供前端同事消费） |

### 1.2 本任务不做什么

| 不做 | 归属 |
|------|------|
| HTTP `Client` 复用 | 任务 08（独立改进，先做后做都行） |
| React/Vue 组件层改造 | **前端同事**按 §7 的 TS 类型实施（用户是后端） |
| QUIC 协议加消息 `id` 字段 | **不做** —— 协议版本兼容成本高，用「已读低估自愈」语义替代（见 §5.1） |
| 通知同步（`get_unread_notification`） | 不动 |

---

## 2. 前置

- [ ] 服务端任务 01–06 完成（7 个 `/session/*` 接口可用）
- [ ] 从服务端仓库**物理拷贝** `crates/common/testdata/session_uuid_vectors.json` → `src-tauri/testdata/session_uuid_vectors.json`
- [ ] `src-tauri/Cargo.toml` 的 `uuid` 依赖**添加 `v5` feature**（客户端当前只用了 v4）

```toml
# 改前(实际 features 以 Cargo.toml 为准, 勿照抄)
uuid = { version = "1", features = ["v4"] }
# 改后
uuid = { version = "1", features = ["v4", "v5"] }
```

---

## 3. v5 派生复刻与跨端断言

### 3.1 新增 `src-tauri/src/utils/session_uuid.rs`

```rust
//! 会话标识派生(服务端 common::utils::session_uuid 的逐行复刻)。
//! ⚠️ 算法与 CONV_NAMESPACE 必须与服务端严格一致 —— 由 testdata 向量断言守护。

use uuid::Uuid;

/// 与服务端 config_str::CONV_NAMESPACE_STR 同值(从向量文件核对)。
pub const CONV_NAMESPACE_STR: &str = "<服务端任务01生成的值>";

/// 单聊会话标识: 字节序规范化 + v5 派生, 双向对称。
pub fn single_session_uuid(a: &Uuid, b: &Uuid) -> Uuid {
    let (lo, hi) = if a.as_bytes() <= b.as_bytes() { (a, b) } else { (b, a) };
    Uuid::new_v5(&namespace(), format!("{lo}:{hi}").as_bytes())
}

fn namespace() -> Uuid {
    Uuid::parse_str(CONV_NAMESPACE_STR).expect("CONV_NAMESPACE_STR 非法")
}
```

注册进模块树（`utils/mod.rs`）。可选：包一层 `#[tauri::command] pub fn session_uuid_cmd(a: String, b: String) -> Result<String, String>` 暴露给前端做会话 key —— 建议提供。

### 3.2 跨端向量断言（本任务最重要的测试）

```rust
// src-tauri/src/utils/session_uuid.rs 内联 tests
#[cfg(test)]
mod tests {
    use super::*;
    use serde::Deserialize;

    #[derive(Deserialize)]
    struct VectorCase { a: String, b: String, expected: String }

    #[derive(Deserialize)]
    struct Vectors { cases: Vec<VectorCase> }

    /// ⚠️ 本测试是"两端派生一致"的唯一守护 —— 向量文件由服务端任务01产出。
    #[test]
    fn matches_server_vectors() {
        let raw = std::fs::read_to_string(concat!(
            env!("CARGO_MANIFEST_DIR"), "/testdata/session_uuid_vectors.json"
        )).expect("缺少向量文件: 从服务端仓库拷贝");
        let v: Vectors = serde_json::from_str(&raw).expect("向量文件格式错误");
        assert!(!v.cases.is_empty());
        for c in v.cases {
            let got = single_session_uuid(
                &Uuid::parse_str(&c.a).unwrap(),
                &Uuid::parse_str(&c.b).unwrap(),
            );
            assert_eq!(got.to_string(), c.expected, "case a={} b={}", c.a, c.b);
        }
    }
}
```

> `serde_json` 在 src-tauri 依赖中已有。此测试不需要网络/数据库 —— CI 可跑。

---

## 4. 本地表改造

### 4.1 `chat_session`（`src-tauri/src/entity/chat_session.rs`）

`ChatSession` 结构体与 `create_table` / `update_table`（迁移）加两列，`UNIQUE` 调整：

```sql
CREATE TABLE IF NOT EXISTS chat_session (
    ... 现有全部列 ...,
    group_id TEXT,
    session_uuid TEXT,                       -- 新增: 单聊 v5 派生 / 群聊 = group_uuid
    synced_id INTEGER NOT NULL DEFAULT 0,    -- 新增: 本地已拉取位置
    UNIQUE(session_uuid)
)
```

`update_table` 迁移数组追加（沿用现有「失败忽略」模式）：

```rust
"ALTER TABLE chat_session ADD COLUMN session_uuid TEXT DEFAULT NULL",
"ALTER TABLE chat_session ADD COLUMN synced_id INTEGER NOT NULL DEFAULT 0",
"CREATE UNIQUE INDEX IF NOT EXISTS idx_chat_session_su ON chat_session(session_uuid)",
```

> 未上线无历史数据 —— 存量行回填可跳过（直接重建本地库）。若仍想回填：单聊 `session_uuid = single_session_uuid(me, send_user)`、群聊 `= group_id`。

`ChatSessionVo`（`src-tauri/src/vo/chat_session_vo.rs`）同步加 `session_uuid: String`、`synced_id: i64`。

### 4.2 `chat_record` / `group_chat_record` 加 `server_id`

```sql
ALTER TABLE chat_record ADD COLUMN server_id INTEGER DEFAULT NULL;      -- 同步消息的服务端 id; 在线消息为 NULL
ALTER TABLE group_chat_record ADD COLUMN server_id INTEGER DEFAULT NULL;
```

实体结构体加 `pub server_id: Option<i64>`，`insert_chat_record` / `GroupChatRecord::insert` 的 INSERT 列同步加。

**`server_id` 的语义（重要）**：

- 经 `/session/sync` 拉到的消息：填 `SyncMessageVO.id`
- 在线 QUIC 推送的消息：**NULL**（协议无 id 字段，§1.2 已决策不改协议）
- 已读上报取「该会话本地 max(server_id)」—— 在线消息无 id 意味着**已读可能低估**（详见 §5.1）

### 4.3 `chat_record_read`（本地阅读事件表）

**保留不动** —— `send_read_message` 的 10 秒批量节流水位逻辑继续用它，仅上报格式切换。

---

## 5. 同步流程重写

### 5.1 已读低估自愈（先讲清语义，这是本任务最关键的决策）

新契约报 `last_read_id`（服务端消息 id），但在线消息本地无 id。设计：

- 客户端报「该会话本地 **max(server_id)**」—— 只统计已同步落库的消息
- 在线读了未同步（无 id）的消息 → 本次上报**跳过或低估**
- **低估无害**：服务端 `last_read_id` 偏小 → 服务端未读数偏大，但**客户端角标是本地算的**（`is_new && is_received`），界面无感知
- **自愈**：下次同步把 `server_id` 带入 → 下次上报推进到位
- **宁少勿过**：反过来「报过头」会把还没同步下来的消息标已读、丢角标 —— 本设计从机制上杜绝

### 5.2 `sync_offline_messages` 重写（`user_service.rs:480`）

```rust
pub async fn sync_offline_messages() {
    set_is_syncing(true).await;

    // 通知同步保留(不动)
    get_unread_notification().await.unwrap_or_else(|e| error!("拉取未读通知失败 {:?}", e));

    // 会话消息同步(新)
    if let Err(e) = sync_sessions_offline().await {
        error!("离线消息同步失败: {:?}", e);
    }
    // 会话列表刷新(新): 补齐 pin/mute/软删/服务端未读
    if let Err(e) = refresh_session_list().await {
        error!("刷新会话列表失败: {:?}", e);
    }

    set_is_syncing(false).await;
}
```

### 5.3 `sync_sessions_offline`（核心循环）

```rust
/// /session/sync 空响应终止循环(主方案 §8.3 / 任务05 §2.3 协议)。
async fn sync_sessions_offline() -> Result<(), anyhow::Error> {
    let me = get_user_info("uuid").await?;

    // 首次判定: 本地存在任何 synced_id > 0 的会话 → 增量; 否则 initial
    let mode = if has_any_synced_session(&me).await? { "incremental" } else { "initial" };

    loop {
        let body = serde_json::json!({ "mode": mode, "sessions": [], "limit": 100 });
        let resp = post_request(format!("{}/session/sync", TALK_API), body.to_string()).await?;
        let result: HttpResult = parse_http_result(&resp.body)?;
        if result.code != 200 || result.data.is_null() {
            break;
        }
        let sync: SyncResponse = serde_json::from_value(result.data)?;
        if sync.sessions.is_empty() {
            break;   // 终止条件(§2.3): 无未同步会话
        }

        let mut acks: Vec<serde_json::Value> = Vec::new();
        for s in &sync.sessions {
            for msg in &s.messages {
                if s.session_type == 1 {
                    insert_single_sync_message(msg).await?;      // §5.4
                } else {
                    insert_group_sync_message(msg).await?;       // §5.4
                }
            }
            // 本地会话行: session_uuid + synced_id 落库
            update_local_session_cursor(&me, &s.session_uuid, s.next_cursor).await?;
            acks.push(serde_json::json!({
                "session_uuid": s.session_uuid,
                "synced_id": s.next_cursor,
            }));
        }

        // 先落库再回报(宁重勿漏; 回报失败 → 下轮重拉, nano_id 去重兜住)
        post_request(
            format!("{}/session/synced", TALK_API),
            serde_json::to_string(&serde_json::json!({ "sessions": acks }))?,
        ).await?;
    }
    Ok(())
}
```

> `has_more=true` 的会话**无需特殊处理**：回报 `synced_id` 后，下轮服务端筛选「有未同步」自动包含它（`id > synced_id` 仍成立）—— 循环天然继续。`has_more` 仅用于 UI 进度提示。

### 5.4 消息分流落库

```rust
/// 单聊: SyncMessageVO → TextQuicMsgVo → 复用现有 insert_chat_record 去重链
async fn insert_single_sync_message(msg: &SyncMessage) -> Result<(), anyhow::Error> {
    let vo = TextQuicMsgVo {
        nano_id: msg.nano_id.clone(),
        text_type: msg.text_type,
        raw: String::from_utf8_lossy(&msg.raw).to_string(),
        recv_user: msg.recv_user.clone(),
        send_user: msg.send_user.clone(),
        timestamp: msg.timestamp,
    };
    // is_new: 本地已有同 nano_id → false(不计角标, §现有逻辑)
    let is_new = insert_chat_record(&vo).await?;
    if is_new && msg.recv_user == get_user_info("uuid").await? {
        bump_local_unread(&msg.send_user, &vo).await?;   // 沿用现有角标累加模式
    }
    // server_id 记录(已读上报用): UPDATE chat_record SET server_id WHERE nano_id = ?
    set_server_id(&msg.nano_id, msg.id).await?;
    Ok(())
}

/// 群聊: recv_user 即 group_uuid, text_type 即 msg_type(任务05 §2.3 契约)
async fn insert_group_sync_message(msg: &SyncMessage) -> Result<(), anyhow::Error> {
    let record = GroupChatRecord {
        id: 0,
        nano_id: msg.nano_id.clone(),
        text_type: msg.text_type,
        raw: String::from_utf8_lossy(&msg.raw).to_string(),
        group_id: msg.recv_user.clone(),        // 群标识在 recv_user
        send_user: msg.send_user.clone(),
        timestamp: msg.timestamp,
    };
    let is_new = GroupChatRecord::insert(&record).await?;   // 现有去重
    if is_new && msg.send_user != get_user_info("uuid").await? {
        bump_local_group_unread(&msg.recv_user, &record).await?;
    }
    set_group_server_id(&msg.nano_id, msg.id).await?;
    Ok(())
}
```

> `bump_local_unread` / `update_local_session_cursor` 为 `session_db.rs` 新增 DAO（按 `session_uuid` 定位更新，替代旧 `update_chat_session_db` 的 `(send_user, recv_user)` 定位；旧函数保留 —— 通知等其他路径还在用）。

### 5.5 删除旧同步函数

| 位置 | 动作 |
|------|------|
| `user_service.rs:160`（`get_unread_message`） | **删**（连同 `TextQuicMsg` 反序列化分支；`insert_chat_record` 依赖保留） |
| `group_service.rs:409`（`pull_group_messages`） | **删** |

### 5.6 `refresh_session_list`

```
loop {
    POST /session/list {cursor, size: 50}
    for vo in sessions: upsert 本地 chat_session
        (session_uuid / last_message / last_preview / unread / pinned / muted)
    has_more? 取 next_cursor 继续 : break
}
```

> 与 `query_chat_session_db`（本地列表查询）并存：本地仍是 UI 主数据源，`refresh` 只是把服务端状态（尤其 pin/mute/软删）合并进本地。前端 `useQuicDisconnect` 的 `quic_sync_complete` 监听里触发一次列表刷新（前端同事实施，见 §7）。

---

## 6. 已读上报切换

### 6.1 `send_read_message` 改造（`user_service.rs:342-395`）

**骨架保留**（10 秒循环、`is_syncing` 抑制、本地 `chat_record_read` 水位），仅替换上报段：

```rust
// 改前: 构造 Vec<AddReadChatRecord> → POST /msg/add_read_chat_record
// 改后: 按"会话"聚合, 每会话报 max(server_id)

let me = get_user_info("uuid").await?;
let mut reads: Vec<serde_json::Value> = Vec::new();

// 单聊: 阅读事件按对方聚合 → 派生 session_uuid → 本地 max server_id
for peer in distinct_peers_from_read_events(&uuid, &mut timestamp).await? {
    let su = single_session_uuid(&me_uuid, &peer).to_string();
    if let Some(max_id) = local_max_server_id(&su).await? {
        if max_id > 0 {
            reads.push(json!({ "session_uuid": su, "session_type": 1, "last_read_id": max_id }));
        }
    }
}
// 群聊: group_uuid 即 session_uuid, 同聚合(水位 group_timestamp 沿用)
for group in distinct_groups_from_read_events(&uuid, &mut group_timestamp).await? {
    if let Some(max_id) = local_max_group_server_id(&group).await? {
        if max_id > 0 {
            reads.push(json!({ "session_uuid": group, "session_type": 2, "last_read_id": max_id }));
        }
    }
}

if !reads.is_empty() {
    post_request(
        format!("{}/session/read", TALK_API),
        serde_json::to_string(&json!({ "reads": reads }))?,
    ).await?;
    // 水位推进逻辑沿用(成功后 timestamp/group_timestamp 已在聚合时前移)
}
```

> `AddReadChatRecord` DTO 与 `/msg/add_read_chat_record` 调用**删除**（`dto/add_read_chat_record.rs` 删或标记废弃 —— 服务端桥接在客户端发版后由任务 08 前后删除）。

### 6.2 已读低估的自愈路径（与 §5.1 呼应）

在线读消息（无 server_id）→ 本次循环跳过 → 角标本地算无感知 → 下次同步带入 server_id → 下次上报推进。**端到端最终一致，无丢失场景**。

---

## 7. TS 类型（`packages/types/src/`）

供前端同事消费（结构与任务 04–06 的服务端 VO 逐字段对齐）：

```typescript
// packages/types/src/session.ts
export interface SessionListItem {
  session_uuid: string;
  session_type: 1 | 2;
  peer_uuid: string | null;      // 单聊: 对方; 群聊: null(用 session_uuid 查群资料)
  last_message_id: number;
  last_message_at: number;
  last_preview: string;
  pinned: number;                // 0 | 1
  muted: number;                 // 0 | 1
  unread: number;
}

export interface SessionListResponse {
  sessions: SessionListItem[];
  has_more: boolean;
  next_cursor: { pinned: number; last_message_at: number; session_uuid: string } | null;
}

export interface SyncMessage {
  id: number;
  nano_id: string;
  session_uuid: string;
  session_type: 1 | 2;
  send_user: string;
  recv_user: string;             // 群聊时即 group_uuid
  text_type: number;             // 单聊/群聊值域不同, 按 session_type 解释
  timestamp: number;
  raw: number[];
}
```

> `raw: number[]` 与现有接口一致；前端做 `String.fromCharCode`/UTF-8 解码的既有逻辑可复用。

前端实施要点（交前端同事）：`quic_sync_complete` 事件后触发列表刷新；会话组件以 `session_uuid` 为 React key（替代现在复用 `send_user` 的做法）；pin/mute/delete 按钮接三个 POST。

---

## 8. 测试与联调

### 8.1 Rust 单测（src-tauri，CI 可跑）

| 测试 | 内容 |
|--------|------|
| `matches_server_vectors`（§3.2） | **跨端一致性守护** —— 全向量断言 |
| `sync_mode_initial_first_time` | 本地无 synced>0 会话 → mode = initial |
| `sync_response_parse` | 响应 JSON → `SyncResponse` 结构完整解析（fixture 文件） |
| `read_report_skip_zero` | `max(server_id) = 0` 的会话不出现在上报列表 |

### 8.2 联调验收（前后端 + 手动）

```
1. 跨端向量: cargo test -p <src-tauri 包名> matches_server_vectors 绿 ✓
2. 新设备登录 → initial 同步 → 本地消息/会话/角标正确
3. 断网发消息(对端) → 重连 → 增量同步: 消息补齐、角标只加新到、无重复
4. 1000+ 条离线消息(压测脚本) → has_more 分批拉全、耗时在同步遮罩(3min)内
5. 在线读消息 → ≤20s 后服务端 user_session.last_read_id 推进(pg 查询)
6. 离线超 7 天(改库造数据) → truncated_by_window=true、界面提示
7. 置顶/免打扰/软删 → 界面状态与服务端一致; 软删后新消息复活
8. 群聊入群(邀请接受) → 历史不计未读(§7.4 游标初始化)
9. 旧接口零调用: 客户端日志中无 /msg/get_unread_chat_record 与 /msg/add_read_chat_record 请求
```

> 前端无单测（用户明确此项目不做前端单测，手动验收为准）。

---

## 9. 验收（DoD）

- [ ] 跨端向量断言绿（两仓库的向量文件为同一份）
- [ ] 本地三张表加列完成、迁移幂等
- [ ] 同步循环走通（initial + 增量 + has_more 分批 + 空响应终止）
- [ ] 已读上报走 `/session/read`，节流骨架保留，低估自愈语义经联调第 5 项验证
- [ ] `get_unread_message` / `pull_group_messages` / `AddReadChatRecord` 调用删除
- [ ] 会话列表刷新接 `quic_sync_complete`
- [ ] TS 类型就位（前端可开工）
- [ ] 联调 9 项全过

---

## 10. 本任务产出

| 产出 | 说明 |
|------|------|
| 客户端新同步链路 | 服务端桥接层（`/msg/add_read_chat_record` 旧语义 + `/msg/get_unread_chat_record` 临时版）进入**待删除状态** |
| `session_uuid` 本地化 | 会话 React key / 跳转参数的稳定标识 |
| 联调验收记录 | 任务 08 删除旧接口的依据（第 9 项：旧接口零调用） |

---

## 11. 系列任务规划（更新）

| 任务 | 主题 | 状态 |
|------|------|------|
| 01–06 | 服务端全链路 | 文档就绪 |
| **07（本档）** | 客户端改造 | 文档就绪 |
| 08 | 旧接口删除（桥接 + 临时版）+ 历史接口入参 session_uuid 化 + HTTP `Client` 复用 | 待写 |
| 09 | 分区验证 + 清理 + 定时兜底聚合 | 待写 |
