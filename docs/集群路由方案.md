# IM 集群路由方案

## 1. 目标

将现有的单机 IM 系统升级为**多节点集群**，实现：

- 外网 QUIC 节点**只负责客户端连接管理与本机投递**，不参与节点间通信
- 内网 QUIC 节点作为**集群统一路由层**，承担所有跨节点转发（HTTP 通知 + QUIC 消息转发）
- 节点间通信采用 **新流 + 请求/响应** 模式，利用现有 `InternalQuicRequest/Response` 协议
- **确定性用户分配**：通过 `hash(uuid) % server_count` 将用户固定分配到指定外网 QUIC 节点
- 节点自动发现，通过 Redis 注册，有序号索引

---

## 2. 架构概览

```
                     ┌──────────────────────────────────────────────┐
                     │              Redis 注册中心                    │
                     │                                              │
                     │  外网 QUIC 节点注册（按序号）：                  │
                     │    QUIC:SERVER:EXTERNAL:0                     │
                     │      → "192.168.1.10:4433"                   │
                     │    QUIC:SERVER:EXTERNAL:1                     │
                     │      → "192.168.1.20:4433"                   │
                     │                                              │
                     │  内网 QUIC 节点注册（按序号）：                  │
                     │    INTERNAL:QUIC:SERVER:0                     │
                     │      → "192.168.1.10:4434"                   │
                     │    INTERNAL:QUIC:SERVER:1                     │
                     │      → "192.168.1.20:4434"                   │
                     │                                              │
                     │  客户端路由（用户 → 外网节点序号）：             │
                     │    PC:QUIC:SERVER:user1:TEXT → "0"            │
                     │    PC:QUIC:SERVER:user2:TEXT → "1"            │
                     └──────────────────────────────────────────────┘
                                  ▲                  │
                                  │                  │ 查 Redis
                                  │                  ▼
┌─────────────────────────────────┼──────────────────┼─────────────────────┐
│                        Node 0                        │                     │
│  server_index = 0                                    │                     │
│                                                      │                     │
│  ┌──────────────────────────┐    ┌───────────────────┴─────────────────┐ │
│  │  外网 QUIC (:4433)        │    │  内网 QUIC (:4434) — 集群统一路由层  │ │
│  │  只做客户端连接 + 本机投递  │    │                                    │ │
│  │                          │    │  ┌────────────────────────────────┐ │ │
│  │  connections DashMap     │    │  │ route_request(target_user)      │ │ │
│  │    user1 → QuicConn      │    │  │                                │ │ │
│  │                          │    │  │  preferred_index=hash(uuid)%N│ │ │
│  │  send_msg_to_user()      │    │  │                                │ │ │
│  │    ├─ 本机找到 → 直接投递 │    │  │  if preferred_index == 本机序号:│ │ │
│  │    └─ 本机未找到         │    │  │     → 查本机 conns → 投递       │ │ │
│  │       → 发给本机内网 QUIC │───►│  │  else:                         │ │ │
│  └──────────────────────────┘    │  │     → Redis 取目标地址           │ │ │
│                                  │  │     → 转发到目标内网 QUIC        │ │ │
│  ┌──────────────────────────┐    │  └────────────────────────────────┘ │ │
│  │  HTTP API (Actix)        │    └────────────────────────────────────┘ │
│  │  用户注册/好友/文件等     │                       │                     │
│  │                          │                       │                     │
│  │  GET /quic_servers       │                       │                     │
│  │    hash(uuid)%N → 只返   │                       │                     │
│  │    回分配的节点           │                       │                     │
│  │                          │                       │                     │
│  │  需要推送通知时           │                       │                     │
│  │  → 发给本机内网 QUIC ────┼───────────────────────┘                     │
│  └──────────────────────────┘                                             │
└───────────────────────────────────────────────────────────────────────────┘
                                  │
                                  │ 内网 QUIC 跨节点转发
                                  ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                        Node 1                                              │
│  server_index = 1                                                         │
│                                                                           │
│  ┌──────────────────────────┐    ┌───────────────────────────────────────┐│
│  │  外网 QUIC (:4433)        │    │  内网 QUIC (:4434)                    ││
│  │  connections DashMap     │◄───│  hash 确认是本机 → 投递到客户端        ││
│  │    user2 → QuicConn      │    │                                       ││
│  └──────────────────────────┘    └───────────────────────────────────────┘│
└───────────────────────────────────────────────────────────────────────────┘
```

> **三个关键设计**：
> 1. 外网 QUIC 只做"本机客户端 ↔ 本机内网 QUIC"的桥接
> 2. 内网 QUIC 是集群的**统一路由层**，通过 `hash(uuid) % N` 定位目标节点
> 3. HTTP API 返回给客户端的只是**分配给该用户的唯一节点**，不是全部节点列表

---

## 3. 确定性分配：`hash(uuid) % server_count`

### 3.1 原则

- 每个用户通过 UUID 的 hash 取模，被**确定性**分配到一个外网 QUIC 节点
- 同一用户的所有设备（PC / Mobile）连接**同一个**外网 QUIC 节点
- 路由转发时，直接 `hash(target_uuid) % server_count` 得到目标节点序号，无需遍历

### 3.2 Hash 函数

```rust
use std::collections::hash_map::DefaultHasher;
use std::hash::{Hash, Hasher};

fn compute_server_index(uuid: &str, server_count: u32) -> u32 {
    let mut hasher = DefaultHasher::new();
    uuid.hash(&mut hasher);
    let hash = hasher.finish();
    (hash as u32) % server_count
}
```

> 使用标准库 `DefaultHasher`，足够均匀且无外部依赖。

### 3.3 用户连接流程

```
1. 客户端请求 HTTP API: GET /quic_servers
     请求头携带 JWT token → 解析出 uuid
     
2. HTTP API 计算: index = hash(uuid) % server_count
     查 Redis: QUIC:SERVER:EXTERNAL:{index}
     返回: { "index": 0, "address": "192.168.1.10:4433" }

3. 客户端使用返回的 address 连接外网 QUIC

4. 外网 QUIC 鉴权后，注册到 Redis:
     PC:QUIC:SERVER:user1:TEXT → "0" (只存序号，不存地址)
```

---

## 4. 连接类型：双端口各司其职

### 4.1 端口职责

| 端口 | 服务 | 连接方 | 职责 |
|------|------|--------|------|
| `:4433` | 外网 QUIC | 客户端 App | 1. 客户端鉴权与连接管理<br>2. 接收/投递本机客户端消息<br>3. 本机找不到目标时 → 发给本机内网 QUIC |
| `:4434` | 内网 QUIC | HTTP API +<br>外网 QUIC | 1. 接收所有转发请求<br>2. `hash(target_uuid) % N` 定位目标节点<br>3. 本机 → 直接投递；其他节点 → 转发 |

### 4.2 外网 QUIC 不变的部分

外网 QUIC 的现有逻辑**绝大部分无需改动**：

- `handle_connection()` 保持 JSON `FirstQuicMsg` 鉴权流程
- bidi stream 接收循环 + uni stream 接收循环不变
- 粘包协议（`HeadMsg` + `TextQuicMsg`）不变
- `connections` DashMap 结构不变

### 4.3 客户端改动

客户端 App 连接外网 `:4433` 的协议完全不变。唯一变化：不再从 HTTP API 拿"服务器列表"，而是拿"分配给自己的唯一节点地址"。

---

## 5. 配置 + 动态 server_count

### 5.1 配置文件（静态部分）

每个节点只需配置自己的序号和地址，`server_count` 不从配置读取：

```toml
[cluster]
server_index = 0                     # 本节点序号，集群内唯一，不可重复

[quic_server]
address = "0.0.0.0:4433"
server_name = "127.0.0.1:4433"       # TLS SNI（不变）
node_address = "192.168.1.10:4433"   # 本节点供客户端连接的地址

[internal_quic_server]
address = "0.0.0.0:4434"
server_name = "INTERNAL_SERVER_1"
node_address = "192.168.1.10:4434"   # 本节点内网 QUIC 地址
```

### 5.2 `server_count` 动态获取

节点总数**不写在配置文件**中，而是从 Redis 同步。实际实现为 **TTL 心跳注册 + SCAN 计数** 模型（非初稿的 INCR/DECR 计数）：

- 节点启动时 `register_external_node()` 用 `set_ex("QUIC:SERVER:EXTERNAL:{index}", node_address, 120)` 注册外网地址（TTL 120s），并每 60s 续期
- `get_cluster_server_count()` 通过 `SCAN QUIC:SERVER:EXTERNAL:*` 统计 key 数作为节点总数
- 内网地址 `INTERNAL:QUIC:SERVER:{index}` 同样注册，仅在优雅退出时由 `unregister_from_redis()` 显式删除；外网 key 靠 TTL 自然过期（宕机节点无需手动清理）

**Redis 存储**：

| Key | Value | 写入方 | 写入时机 |
|-----|-------|--------|---------|
| `QUIC:SERVER:EXTERNAL:{index}` | `{node_address}` | 每个节点 | 启动 `set_ex`（TTL 120s），每 60s 续期；关闭时不显式删除 |
| `INTERNAL:QUIC:SERVER:{index}` | `{node_address}` | 每个节点 | 启动时写入；优雅退出时显式删除 |

### 5.3 后台同步任务

```rust
use std::sync::atomic::{AtomicU32, Ordering};
use std::sync::Arc;

/// 全局 server_count，lock-free 读取
pub static SERVER_COUNT: LazyLock<Arc<AtomicU32>> =
    LazyLock::new(|| Arc::new(AtomicU32::new(1)));

/// 启动后台同步任务（每个节点启动时调用一次）
pub fn start_server_count_sync(redis_pool: Pool, server_index: u32, node_address: String) {
    tokio::spawn(async move {
        // 两个后台任务：
        // 1. 每 60s 续期本节点外网注册（set_ex TTL 120s）
        // 2. 每 10s SCAN QUIC:SERVER:EXTERNAL:* 计数，更新 SERVER_COUNT
    });
}

/// 任意位置读取当前 server_count
fn get_server_count() -> u32 {
    SERVER_COUNT.load(Ordering::Relaxed)
}
```

> 实现见 `crates/common/src/utils/server_count_sync.rs`。使用 `AtomicU32` 而非 `Arc<RwLock<u32>>`，读取零锁开销。路由函数是热点路径。

### 5.4 `ChatNodeConfig` 扩展

```rust
pub struct ChatNodeConfig {
    pub bind_address: SocketAddr,
    pub cert_path: String,
    pub key_path: String,
    pub max_connections: usize,
    pub max_buffer_length: usize,
    pub idle_timeout_secs: u64,
    pub max_concurrent_uni_streams: u8,
    pub server_name: String,            // TLS SNI（不变）
    pub server_index: u32,              // 新增：节点序号
    pub node_address: String,           // 新增：客户端连接地址
    // server_count 不再在此，从 AtomicU32 读取
}
```

> 节选。实际结构体（`crates/quic_service/src/external/config.rs`）另有 `cert_watch_interval_secs`、`cert_expiry_warning_days`、`cert_expiry_check_interval_secs` 三个 TLS 证书监控字段。

### 5.5 各节点部署示例

| 节点 | `server_index` | `node_address` (quic) | `node_address` (internal) |
|------|:---:|------|------|
| Node 0 | 0 | `192.168.1.10:4433` | `192.168.1.10:4434` |
| Node 1 | 1 | `192.168.1.20:4433` | `192.168.1.20:4434` |
| Node 2 | 2 | `192.168.1.30:4433` | `192.168.1.30:4434` |

> 新节点加入只需配一个新的 `server_index`，启动后自动注册到 Redis（120s TTL + 60s 续期）。其余节点 10s 内感知到 `server_count` 变化。

---

## 6. 路由转发流程

> **⚠️ 实际接线状态**：本节的「两阶段路由 + Redis 兜底」是**设计方案**。代码中的 `internal_router.rs`（`route_request` / `route_internal_request`）实现了该方案，但**全仓库无任何调用者（死代码）**。实际接线的是 `internal/internal_quic_server.rs::handle_internal_request()`：群聊广播 → `process_group_broadcast()`；单聊请求 → 仅查本机 DashMap 投递，**不跨节点路由**。真正的跨节点发送在发送方完成（见 6.4），单跳直达首选节点，无中间跳递减、无 Redis 兜底。以下流程为设计稿，供后续接线参考。

### 6.1 路由策略：hash 取模优先 + Redis 兜底

```
路由决策树：

  1. hash(uuid) % server_count → 首选节点序号
          │
          ▼
  2. 首选节点 == 本机？
     ├── 是 → 查本地 connections
     │        ├── 在线 → 投递 ✅
     │        └── 不在线 → 进入第 3 步（Redis 兜底）
     │
     └── 否 → 转发到首选节点的内网 QUIC
               └── 对端同样走第 2 步
                    ├── 在线 → 投递 ✅
                    └── 不在线 → 返回 user_offline
                                   │
                                   ▼
  3. Redis 兜底：查 PC:QUIC:SERVER:{uuid}:TEXT
          │
          ├── 有值（用户在另一个节点在线）
          │     → 转发到实际节点的内网 QUIC
          │     → 投递 (ttl 耗尽则放弃)
          │
          └── 无值（用户确实不在线）
                → 返回 user_offline
```

> **为什么需要 Redis 兜底**：在节点扩缩容或节点故障时，用户可能连接到非 hash 分配的节点（客户端重新获取地址后连接到了其他节点）。hash 取模是首选路由，Redis 兜底覆盖异常情况。当前实现未包含兜底：`send_msg_to_user_by_platform()` 本机未命中后直接读 `INTERNAL:QUIC:SERVER:{preferred_index}` 单跳转发，对方内网 server 不在线即回 `user_offline`。

### 6.2 核心路由函数

```rust
/// 根据目标用户 UUID 计算首选节点序号（从 AtomicU32 读 server_count）
fn compute_preferred_index(uuid: &str) -> u32 {
    let sc = SERVER_COUNT.load(Ordering::Relaxed);
    if sc <= 1 { return 0; }
    let mut hasher = DefaultHasher::new();
    uuid.hash(&mut hasher);
    (hasher.finish() as u32) % sc
}

/// 查 Redis 获取某序号节点的内网 QUIC 地址
async fn get_internal_addr_by_index(index: u32) -> Result<SocketAddr> {
    let key = format!("INTERNAL:QUIC:SERVER:{}", index);
    let addr_str: String = redis_get(&key).await?;
    addr_str.parse()
}

/// Redis 兜底：查用户实际在哪个节点在线
async fn get_actual_node_index(uuid: &str, platform: &str) -> Result<Option<u32>> {
    let key = format!("{}:QUIC:SERVER:{}:{}", platform, uuid, "TEXT");
    let index_str: Option<String> = redis_get_opt(&key).await?;
    Ok(index_str.and_then(|s| s.parse().ok()))
}
```

### 6.3 内网 QUIC 路由（核心 — 两阶段）

> ⚠️ **未接线**：本函数位于 `crates/quic_service/src/internal/internal_router.rs`（实现与下方伪代码基本一致，另带 `core: &CoreState` 参数），但无任何调用者。实际收包入口 `handle_internal_request()` 内联做 bincode 双分支分发，不走本函数。

```rust
async fn route_request(
    request: &InternalQuicRequest,
    connections: &Arc<DashMap<String, QuicConnection>>,
    server_index: u32,
) -> Result<InternalQuicResponse> {
    let preferred_index = request.preferred_index;

    // ===== 阶段一：hash 取模路由 =====

    // 首选节点 == 本机 → 尝试本机投递
    if preferred_index == server_index {
        if let Some(resp) = try_deliver_local(request, connections).await? {
            return Ok(resp);  // 在线，投递成功
        }
        // 本机未找到 → 不走 else 分支，直接进入 Redis 兜底
    } else {
        // 首选节点 ≠ 本机 → 先转发到首选节点试试
        if request.ttl > 0 {
            let target_addr = get_internal_addr_by_index(preferred_index).await?;
            let mut forward_req = request.clone();
            forward_req.ttl -= 1;
            let resp = forward_to_remote(&target_addr, &forward_req).await?;
            if resp.delivered == Some(true) {
                return Ok(resp);  // 首选节点投递成功
            }
        }
    }
    // 首选路由未命中 → 进入 Redis 兜底

    // ===== 阶段二：Redis 兜底 =====

    if request.ttl == 0 {
        return Ok(InternalQuicResponse::user_offline());
    }

    // 查用户实际在哪个节点在线
    let actual_index = get_actual_node_index(&request.target_user, &request.platform).await?;

    match actual_index {
        Some(idx) if idx == server_index => {
            // 实际就在本机（之前阶段一没找到？再试一次）
            try_deliver_local(request, connections).await
                .map(|r| r.unwrap_or_else(InternalQuicResponse::user_offline))
        }
        Some(idx) => {
            // 转发到实际节点
            let target_addr = get_internal_addr_by_index(idx).await?;
            let mut forward_req = request.clone();
            forward_req.ttl -= 1;
            forward_to_remote(&target_addr, &forward_req).await
        }
        None => {
            // 确实不在线
            Ok(InternalQuicResponse::user_offline())
        }
    }
}
```

### 6.4 外网 QUIC 发送（实际实现）

实际代码位于 `crates/quic_service/src/msg_service/process_msg_service.rs`（`send_msg_to_user` / `send_msg_to_user_by_platform`），与设计稿的差异：

```rust
/// 实际签名：不接收 server_index，也不需要转发调用方传 preferred_index
async fn send_msg_to_user(
    core: &CoreState,
    text_msg: TextQuicMsg,
    platform: &str,
    connections: &Arc<DashMap<String, QuicConnection>>,
) -> anyhow::Result<()> {
    let res = generate_text_msg_with_time(/* ... */)?;
    // preferred_index 由 send_msg_to_user_by_platform 内部调用
    // compute_preferred_index(&recv_user) 自行计算（从 SERVER_COUNT 读取）

    // 1. 先无条件查本机 DashMap（PC / MOBILE 两平台），命中 → 直接投递
    // 2. 本机未命中 → 直接读 Redis INTERNAL:QUIC:SERVER:{preferred_index} 取地址，
    //    单跳发给首选节点的内网 QUIC（bincode 二进制 payload，ttl 初始 3，无中间跳递减）
    // 3. 首选节点内网 server 不在线 → 回 user_offline（无 Redis 兜底）
    // 4. 另有「同步到发送者另一设备」的逻辑（本实现多出，设计稿未提）
    Ok(())
}
```

> 不存在 `get_local_internal_addr()`：转发地址直接从 Redis `INTERNAL:QUIC:SERVER:{preferred_index}` 读取。`server_count` 由 `compute_preferred_index()` 内部从 `SERVER_COUNT` 原子变量读取（`sc <= 1` 时直接返回 0，与设计稿一致）。

### 6.5 完整时序：跨节点消息（hash 命中）

> **实际时序**（单跳）：user1 → Node 0 :4433 → 查本机未命中 → 读 Redis `INTERNAL:QUIC:SERVER:1` 取地址 → 直接发给 Node 1 :4434 → 投递。无中间路由层参与。

```
user1               Node 0 :4433        Node 0 :4434        Redis            Node 1 :4434        Node 1 :4433     user2
  │                     │                    │                  │                  │                    │               │
  │── 消息 ───────────► │                    │                  │                  │                    │               │
  │                     │── hash(user2)%2=1  │                  │                  │                    │               │
  │                     │   ≠ 本机(0)        │                  │                  │                    │               │
  │                     │                    │                  │                  │                    │               │
  │                     │── 内网 QUIC 请求 ─► │                  │                  │                    │               │
  │                     │   {target_user:u2, │                  │                  │                    │               │
  │                     │    preferred_idx:1,│                  │                  │                    │               │
  │                     │    platform:PC,    │                  │                  │                    │               │
  │                     │    ttl: 3}         │                  │                  │                    │               │
  │                     │                    │── idx=1≠0        │                  │                    │               │
  │                     │                    │   阶段一：转发    │                  │                    │               │
  │                     │                    │── GET INTERNAL:QUIC:SERVER:1 ────► │                    │               │
  │                     │                    │◄─ "192.168.1.20:4434"              │                    │               │
  │                     │                    │                                     │                    │               │
  │                     │                    │── 内网 QUIC 请求 (ttl=2) ──────────► │                    │               │
  │                     │                    │                                     │── idx=1==本机      │               │
  │                     │                    │                                     │   阶段一：查本地    │               │
  │                     │                    │                                     │   找到 user2 ────► │── open_uni ─►│
  │                     │                    │                                     │◄─ {delivered:true} │◄─────────────│
  │                     │                    │◄─ {status:"ok"} ─────────────────── │                    │               │
  │                     │◄─ {status:"ok"} ── │                  │                  │                    │               │
  │                     │                    │                  │                  │                    │               │
  │◄─ ACK ──────────── │                    │                  │                  │                    │               │
```

### 6.6 完整时序：hash 未命中 → Redis 兜底

> **⚠️ 当前未实现**：此场景（阶段二 Redis 兜底）在现有代码中不存在——首选节点未命中即返回 `user_offline`。以下时序为设计稿。

```
假设 user2 的 hash 分配是 Node 0，但因 Node 0 之前故障重启，
user2 实际连接到了 Node 1。

user1               Node 0 :4433        Node 0 :4434        Redis            Node 1 :4434        Node 1 :4433     user2
  │                     │                    │                  │                  │                    │               │
  │── 消息 ───────────► │                    │                  │                  │                    │               │
  │                     │── hash(user2)%2=0  │                  │                  │                    │               │
  │                     │   == 本机(0)       │                  │                  │                    │               │
  │                     │── 查本地 conns     │                  │                  │                    │               │
  │                     │   user2 不在本机   │                  │                  │                    │               │
  │                     │                    │                  │                  │                    │               │
  │                     │── 内网 QUIC 请求 ─► │                  │                  │                    │               │
  │                     │   {preferred_idx:0,│                  │                  │                    │               │
  │                     │    ttl: 3}         │                  │                  │                    │               │
  │                     │                    │── idx=0==本机    │                  │                    │               │
  │                     │                    │   查本地 conns   │                  │                    │               │
  │                     │                    │   user2 不在线   │                  │                    │               │
  │                     │                    │                  │                  │                    │               │
  │                     │                    │── 阶段二：Redis 兜底                        │               │
  │                     │                    │── GET PC:QUIC:SERVER:user2:TEXT ──────────► │               │
  │                     │                    │◄─ "1"                                      │               │
  │                     │                    │                  │                  │                    │               │
  │                     │                    │── GET INTERNAL:QUIC:SERVER:1 ────────────► │               │
  │                     │                    │◄─ "192.168.1.20:4434"                      │               │
  │                     │                    │                                             │               │
  │                     │                    │── 内网 QUIC 请求 (ttl=2) ──────────────────► │               │
  │                     │                    │                                             │── idx=1==本机  │               │
  │                     │                    │                                             │   查本地 conns │               │
  │                     │                    │                                             │   找到 user2 ─► │── open_uni ─►│
  │                     │                    │                                             │◄─ {delivered}  │◄─────────────│
  │                     │                    │◄─ {status:"ok"} ─────────────────────────── │               │
  │                     │◄─ {status:"ok"} ── │                  │                          │               │
  │                     │                    │                  │                          │               │
  │◄─ ACK ──────────── │                    │                  │                          │               │
```

### 6.7 HTTP API → 内网 QUIC

**实际实现**：HTTP API 的好友通知等场景（`api` 的 `user_integrated_service.rs` 等）把请求发给配置里的本机 `internal_quic_server`（`127.0.0.1:4434`），该 server 只做**本机投递**、不按 `preferred_index` 转发。设计稿的跨节点路由（`preferred_index` → 转发到首选节点）**未实现**：

```
HTTP API → 构造 InternalQuicRequest { preferred_index: X, ... }（设计稿）
         → 发给本机内网 QUIC
              ├── 阶段一：X == 本机序号 → 本机投递
              │           X != 本机序号 → 转发到首选节点（未实现）
              └── 阶段二（未命中）：Redis 兜底 → 查实际在线节点 → 再次转发（未实现）
```

---

## 7. Redis 注册

### 7.1 Key 设计

| Key 模式 | Value | 用途 | 写入方 |
|----------|-------|------|--------|
| `QUIC:SERVER:EXTERNAL:{index}` | `{node_address}` | **节点地址映射**：序号 → 外网地址（兼作 server_count 计数源，SCAN 统计） | 外网 QUIC 启动 `set_ex`（TTL 120s），每 60s 续期 |
| `INTERNAL:QUIC:SERVER:{index}` | `{node_address}` | **内网地址映射**：序号 → 内网地址 | 内网 QUIC 启动时；优雅退出时显式删除 |
| `PC:QUIC:SERVER:{uuid}:TEXT` | `{index}` | **用户路由**：用户 → 所在节点序号 | 外网 QUIC `set_conn_info()` |

> 无 `CLUSTER:SERVER_COUNT` 独立 key（初稿设计，未实现）；节点总数通过 SCAN 外网注册 key 得出。

### 7.2 注册时机

| 事件 | 操作 |
|------|------|
| QUIC 节点启动 | 外网注册 `QUIC:SERVER:EXTERNAL:{index}`（`set_ex` TTL 120s）；内网注册 `INTERNAL:QUIC:SERVER:{index}`；启动 `start_server_count_sync(redis_pool, server_index, node_address)` |
| 用户上线 | `set_conn_info()` 写 `PC:QUIC:SERVER:{uuid}:TEXT` → `{index}` |
| 用户下线 | `end_server()` 删 `PC:QUIC:SERVER:{uuid}:TEXT` |
| QUIC 节点关闭 | 优雅退出时 `unregister_from_redis()` 删 `INTERNAL:QUIC:SERVER:{index}`；外网 key 靠 120s TTL 自然过期 |

### 7.3 合并注册逻辑

- 外网 QUIC 注册：节点地址（TTL 心跳）+ 用户路由
- 内网 QUIC 注册：内网地址（供跨节点转发用）
- server_count 由 SCAN 外网注册 key 计数维护
- **不重复注册**，各负其责

---

## 8. HTTP API：`GET /quic_servers`

### 8.1 功能

客户端获取**分配给自己的唯一外网 QUIC 节点地址**。

### 8.2 请求

```
GET /integrated/quic_servers
Header: Authorization: Bearer {jwt}
```

### 8.3 处理逻辑

```rust
pub async fn get_quic_server_for_user(uuid: &str) -> Result<QuicServerInfo> {
    // 1. hash 取模定位节点序号（server_count 从 SERVER_COUNT 原子变量读取）
    let index = compute_preferred_index(uuid);

    // 2. 查 Redis 获取该节点的外网地址
    let key = format!("QUIC:SERVER:EXTERNAL:{}", index);
    let address: String = redis_get(&key).await?;

    Ok(QuicServerInfo { index, address })
}
```

### 8.4 响应

```json
{
    "code": 200,
    "data": {
        "index": 0,
        "address": "192.168.1.10:4433"
    }
}
```

> **关键变化**：旧方案返回全部服务器列表，新方案只返回分配给该用户的**一个**节点。

---

## 9. 消息持久化（TODO）

保持现有方案不变。`add_user_chat_record()` 在消息处理流程中调用，后续改为 WAL 模式。

详见旧版 §7。本次不实现。

---

## 10. 启动模式拆分

三个入口：

| 入口 | 职责 | 启动的服务 |
|------|------|-----------|
| `src/main.rs` | 单机模式 | 外网 QUIC + 内网 QUIC + HTTP API |
| `crates/quic_service/src/bin/quic_server.rs` | 集群 QUIC 节点 | 外网 QUIC + 内网 QUIC |
| `crates/api/src/bin/api_server.rs` | 集群 HTTP 节点 | HTTP API（内网 QUIC 客户端） |

---

## 11. 群聊路由

已实现为群聊广播（读扩散扇出），见《群聊广播方案》。不再预留。

---

## 12. 文件变更清单

### 12.1 新增文件

| 文件 | 说明 | 落地状态 |
|------|------|---------|
| `crates/quic_service/src/internal/internal_router.rs` | 内网 QUIC 路由逻辑：`compute_preferred_index()` + `route_request()` + Redis 兜底 | ✅ 已创建，**未接线**（无调用者） |
| `crates/common/src/utils/hash_router.rs` | hash 取模公共函数 | ❌ 未创建；hash 逻辑在 `crates/common/src/utils/server_count_sync.rs::compute_preferred_index()` |
| `crates/quic_service/src/wal/`（mod/writer/reader） | WAL 模块（TODO，占位） | ❌ 未创建 |
| `crates/api/src/bin/api_server.rs` | API HTTP 服务独立二进制入口 | ✅ 已创建 |

### 12.2 修改文件

| 文件 | 改动内容 | 落地状态 |
|------|---------|---------|
| `config/app_config.toml` | 新增 `[cluster]` 节（`server_index`）；`[quic_server]` 新增 `node_address`；`[internal_quic_server]` 新增 `node_address` | ✅ 已完成 |
| `crates/common/src/config_str.rs` | 删除 `SERVER_NAME`；新增 `CLUSTER:SERVER_COUNT` Redis key 常量 | ⚠️ `SERVER_NAME` 已删；`CLUSTER:SERVER_COUNT` 未新增（改用 SCAN 计数） |
| `crates/common/src/utils/internal_quic_msg.rs` | `InternalQuicRequest` 新增 `preferred_index`、`platform`、`source`、`ttl`；`InternalQuicResponse` 新增 `delivered` | ✅ 已完成（原 `crates/entity/src/models/internal_quic_msg.rs`，已迁至 common） |
| `crates/common/src/utils/server_count_sync.rs` | 新增：`SERVER_COUNT` AtomicU32 + 后台同步任务 | ✅ 已完成；实现为 TTL 心跳注册 + SCAN 计数 + 10s 轮询（非 3s 读 key） |
| `crates/quic_service/src/external/config.rs` | `ChatNodeConfig` 新增 `server_index`、`node_address`；移除 `server_count` | ✅ 已完成（另新增 3 个 TLS 监控字段） |
| `crates/quic_service/src/internal/internal_config.rs` | `InternalQuicConfig` 新增 `server_index`、`node_address`；移除 `server_count` | ✅ 已完成 |
| `crates/quic_service/src/external/quic_server.rs` | `set_conn_info()` Redis value 改为 `server_index` | ✅ 已完成 |
| `crates/quic_service/src/external/quic_server.rs` | `send_msg_to_user()` 签名改为接收 `server_index: u32`，hash 取模 + 两阶段内网 QUIC 转发（含 Redis 兜底） | ⚠️ 部分：签名**未**接收 `server_index`（内部自算）；单跳直达首选节点，无 Redis 兜底 |
| `crates/quic_service/src/internal/internal_quic_server.rs` | `handle_internal_request()` 改为调用 `internal_router::route_request()` | ❌ 未实现（内联双分支分发，不走 route_request） |
| `crates/quic_service/src/msg_service/process_msg_service.rs` | 适配 `send_msg_to_user()` 新签名（传 `server_index` 而非 `config`） | ⚠️ 签名未加 `server_index`；改用 `&CoreState` 窄签名 |
| `crates/quic_service/src/msg_service/send_msg.rs` | `send_quic_system_msg()` 改为通过内网 QUIC 路由 | ✅ 已完成 |
| `crates/quic_service/src/init_server.rs` | 启动时注册外网 QUIC 节点到 Redis（按序号）；启动 `start_server_count_sync()` 后台任务 | ✅ 已完成；`INCR CLUSTER:SERVER_COUNT` 未实现，改为 TTL 注册 |
| `crates/quic_service/src/lib.rs` | 添加 `internal_router`、`wal` 模块；导出 `SERVER_COUNT` | ⚠️ 有 `pub mod internal;`；无 `wal`；`SERVER_COUNT` 由 common 导出 |
| `crates/quic_service/Cargo.toml` | 按需补充依赖 | ✅ 已完成 |
| `crates/api/src/service/user_integrated_service.rs` | `get_quic_server_list()` 改为 `get_quic_server_for_user(uuid)`，hash 取模后只返回一个节点（`server_count` 从 `SERVER_COUNT` 读取） | ✅ 已完成 |
| `crates/api/Cargo.toml` | 移除 `quic_service` 依赖；新增 `[[bin]]` | ⚠️ `[[bin]]` 已加；`quic_service` 依赖**未移除**（代码未引用，属残留声明） |

### 12.3 无需改动的文件

| 文件 | 原因 |
|------|------|
| `crates/quic_service/src/models/text_msg.rs` | 客户端 ↔ 节点协议不变 |
| `crates/quic_service/src/msg_service/text_msg_service.rs` | 粘包/序列化逻辑不变 |
| `crates/quic_service/src/external/set_server.rs` | TLS 配置不变 |
| `crates/quic_service/src/external/quic_client.rs` | 客户端无需改动 |
| `crates/common/src/utils/internal_quic_client.rs` | 内网 QUIC 客户端接口不变（请求体新增字段向后兼容；原 `crates/entity/src/utils/` 已迁至 common） |
| `crates/quic_service/src/external/chat_node.rs` | `ServiceLifecycle`（定义于 `external/lifecycle.rs`）不变 |
| `crates/quic_service/src/external/tls_monitor.rs` | TLS 监控不变 |
| `crates/quic_service/src/nat_ip/` | NAT 发现 + P2P 不变 |

---

## 13. 实施步骤

> 以下步骤为方案设计。实际落地情况：步骤 1/2/4/6（部分）/7/12/14 已完成；步骤 5/8/9/10 的「两阶段路由」「INCR/DECR 计数」部分未按设计实施（见 §12 落地状态）。

### 第一阶段：配置 + 动态 server_count

1. `config/app_config.toml` — 新增 `[cluster]` 节（`server_index`）；`[quic_server]`、`[internal_quic_server]` 各新增 `node_address` ✅
2. `config.rs` / `internal_config.rs` — 结构体新增 `server_index`、`node_address`；移除 `server_count` ✅
3. `common/src/utils/server_count_sync.rs` — 实现 `SERVER_COUNT` AtomicU32 + 后台同步任务（实际为 TTL 心跳 + SCAN 计数 + 10s 轮询）✅
4. `internal_quic_msg.rs` — `InternalQuicRequest` 新增 `preferred_index`、`platform`、`source`、`ttl`；`InternalQuicResponse` 新增 `delivered` ✅
5. `config_str.rs` — 新增 `CLUSTER:SERVER_COUNT` 常量 ❌（未实施）
6. `init_server.rs` — 注册外网/内网 QUIC 节点到 Redis（按序号）；调用 `start_server_count_sync()` ✅（`INCR` 未实施，改为 TTL 注册）
7. `quic_server.rs` — `set_conn_info()` Redis value 改为 `server_index` ✅

### 第二阶段：内网 QUIC 两阶段路由改造

8. 新建 `internal_router.rs` — 实现两阶段 `route_request()`：hash 取模 → Redis 兜底 ⚠️ 已创建但**未接线**
9. 改造 `internal_quic_server.rs` — `handle_internal_request()` 改为调用 `route_request()` ❌（内联分发）

### 第三阶段：外网 QUIC 适配

10. `send_msg_to_user()` — 签名改为 `server_index: u32`；hash 取模从 `SERVER_COUNT` 读取；未命中交给内网 QUIC 两阶段路由 ⚠️ 部分：hash 取模 ✅；签名未加 `server_index`，单跳直达无兜底
11. `hash_router.rs` — `compute_preferred_index()` 从 `SERVER_COUNT` 读取 ❌ 未创建独立文件（实现在 `server_count_sync.rs` 内）✅ 逻辑存在

### 第四阶段：HTTP 接口改造

12. `get_quic_server_list()` → 改为 `get_quic_server_for_user(uuid)`：
    - `compute_preferred_index(uuid)` → 查 Redis → 只返回一个节点 ✅
13. `api/Cargo.toml` — 移除 `quic_service` 依赖 ❌（仍声明，代码未引用）

### 第五阶段：独立二进制 + 测试

14. `api/src/bin/api_server.rs` — HTTP 服务独立二进制 ✅
15. 替换 `add_user_chat_record()` 为 TODO 占位（为 WAL 预留）✅（`process_msg_service.rs` 中带 TODO kafka 注释）
16. 端到端测试：两个节点各连客户端，验证 hash 分配 + 跨节点消息转发 ✅（`tests/internal_forwarding_test.rs`）

---

## 14. 方案对比总结

| 维度 | 初版（魔术字节） | 二版（内网路由） | 三版（hash 取模） |
|------|:---:|:---:|:---:|
| 外网 QUIC 职责 | 客户端 + 节点间 | 仅客户端 | 仅客户端 |
| 路由方式 | 魔术字节分发 | 查 Redis 遍历 | **hash 取模一次定位** |
| 用户分配 | 随机（先到先得） | 随机 | **确定性分配** |
| HTTP 返回 | 全部服务器列表 | 全部服务器列表 | **只返回分配的一个** |
| 客户端感知 | 需发魔术字节 | 无变化 | **无变化** |
| 节点间协议 | 新 inter_node | 复用内网 QUIC | 复用内网 QUIC |
| 新增模块 | `inter_node/` | `internal_router.rs` | `internal/internal_router.rs`（两阶段路由待接线） |
